import axios from 'axios'

// need to dyanmically add api keys instead of adding it on the headers on each axios request

export const getBreedsList = (filter = '') => {


  return ''
}

export const getCats = () => {


  return ''
}